@model IEnumerable<Benday.YamlDemoApp.Api.DomainModels.Person>

@{
ViewBag.Title = "Index";
}

    <h2>
    person list
    </h2>

    <p>
    @Html.ActionLink("Search", "Search") | @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table table-striped table-hover">
        <thead class="thead-light">
            <tr>
                <th>
                first name
                </th>
                <th>
                last name
                </th>
                <th>
                phone number
                </th>
                <th>
                email address
                </th>
            
                <th>
                </th>
            </tr>
        </thead>
    
    @foreach (var item in Model) {
        <tr class="clickable-tr" onclick="handleRowClick(this)" data-href="@Url.Action("Edit", new { id = item.Id })">
            <td>
            @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
            @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
            @Html.DisplayFor(modelItem => item.PhoneNumber)
            </td>
            <td>
            @Html.DisplayFor(modelItem => item.EmailAddress)
            </td>
        
            <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
            </td>
        </tr>
    }
    
    </table>