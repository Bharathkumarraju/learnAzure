Resources in Resource Group share same lifecycle.
Resources can only exist in one Resource Group.

Resources can communicate across Resource Groups.
Container for security boundaries.

Can export infrastructure as code using Azure Resource Manager templates(ARM Templates).


Resources can be created in different regions than the resources in the group.


ARM(Azure Resource Manager) is the deployment and management service for Azure.

